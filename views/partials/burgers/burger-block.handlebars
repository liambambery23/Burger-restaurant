<div class="mt-4">
    <h4>{{burger_name}}</h4>

    <button class="change-devour btn btn-warning" data-id="{{id}}" data-newdevour="{{devoured}}"><i class="fas fa-hamburger"></i> Devour it!</button>
</div>